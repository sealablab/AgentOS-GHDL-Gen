
# 2025-11-14-plan-jc

## S1) create `sys/forge_platform`

- [ ] APP_forge_mcc_top.vhd compiles
- [ ] APP_forge_shim.vhd compiles
- [ ] APP_forge_main.vhd compiles : NOTE: should this exist? (probably)


## NOTETOSELF: 
Maybe i should create a WIP/ app_forge

## S2) create `sys/forge_platform_sim`
This requires an in-deph understanding of the moku-models 



## Q1: python support library
art of the new design entails creating a small python utility library that papers over a few of the **L2-Shim**: layer abstractions. these include

#### 'app' level reset

#### 'pushing' that ill-defined `NetworkRegs Bundle`

The biggest question is: __where should this python library live__ ? 
- `/sys/forge-platform-py` ? 
**eventually** this library should be able to live 'outside' of the entire FORGE developer ecosystem (almost like a libc-runtime).. but that is a future concern.


## Q2: 'NetworkAppReg-bundle'
We need to come up with a way to basically represent the same information that lives in BPD-RTL.yml, but in a somehow less domain specific language. 😳

#### [[../../bpd-tiny-vhdl/BPD-RTL.yaml|BPD-RTL]]
``` python

platform: moku_go
description: >
  Generic Basic Probe Driver control surface for multi-probe FI operations.

control_registers:
  # CR0[31:29] reserved for FORGE control scheme (forge_ready, user_enable, clk_enable)
  # CR1-CR11 available for application (11 registers)
  # CR12-CR15 unused (future expansion)

datatypes:
  ###########################################################################
  # 1. Arming and lifecycle management
  ###########################################################################
  # Arm the FSM (IDLE → ARMED transition).
  - name: arm_enable
    datatype: boolean
    description: Enable arming of the FSM (IDLE → ARMED transition).
    default_value: false
    control_register: CR1
    bit_range: "[0]"
```

